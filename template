#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define ld long double

const long long INF = 1e18;
const long long mod = 1000000007;

bool chmax ( ll &a, ll b ) { return a < b ? a = b : false; }
bool chmin ( ll &a, ll b ) { return a > b ? a = b : false; }

#warning need array limit
ll A[long(1e7+5)];
ll B[long(1e7+5)];

void prepare_lookup_table ()
{
}

void do_task ()
{
}

int main ()
{
	ios_base::sync_with_stdio(false);
	cin.tie(0);

	prepare_lookup_table();

	ll t = 1;
	cin >> t;
	while ( t-- ) {
		do_task();
	}


	return 0;
}
